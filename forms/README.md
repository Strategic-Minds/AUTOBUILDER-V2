# forms/

**Not built yet.** Spec calls for main intake / revision request / support request forms.
This is Phase 4 of the buildout (CRM/Forms/Apps Script) and is blocked pending a decision:
build via Google Forms + Apps Script (needs live testing in this environment, not yet
attempted) vs. a form embedded in the eventual client-portal app. Flagged to Jeremy, not
silently skipped.
