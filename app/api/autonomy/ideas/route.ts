import { NextRequest, NextResponse } from 'next/server';
import { requireAuth } from '@/lib/api-auth';
import { enqueueAutonomyTask, listAutonomyJobs } from '@/lib/autonomy/store';
import type { AutonomyTask } from '@/lib/autonomy/types';
export const dynamic = 'force-dynamic';
export async function GET(req: NextRequest) { try { await requireAuth(); return NextResponse.json({ jobs: await listAutonomyJobs(Number(req.nextUrl.searchParams.get('limit') || 50)) }); } catch (error) { if (error instanceof NextResponse) return error; return NextResponse.json({ error: error instanceof Error ? error.message : 'Failed to list ideas' }, { status: 500 }); } }
export async function POST(req: NextRequest) { try { const user = await requireAuth(); const body = await req.json(); const idea = String(body.idea || '').trim(); if (!idea) return NextResponse.json({ error: 'idea is required' }, { status: 400 }); const runId = `run-${crypto.randomUUID()}`; const task: AutonomyTask = { taskId: `task-${crypto.randomUUID()}`, runId, projectId: body.projectId, taskClass: 'idea-intake', objective: idea, input: { project_name: body.projectName, source_refs: body.sourceRefs || [], requirements: body.requirements || {}, requested_outputs: body.requestedOutputs || ['github_repo','vercel_project','validated_preview'] }, allowFallback: true, maxConcurrency: Number(body.maxConcurrency || 8), idempotencyKey: String(body.idempotencyKey || `idea:${user.id}:${runId}`), metadata: { submitted_by: user.id, source: 'dashboard', completion_target: 'vercel-project' } }; const queued = await enqueueAutonomyTask(task, 'autonomous-build', 1); return NextResponse.json({ ok: true, run_id: runId, state: 'IDEA_RECEIVED', queued }, { status: 202 }); } catch (error) { if (error instanceof NextResponse) return error; return NextResponse.json({ error: error instanceof Error ? error.message : 'Failed to submit idea' }, { status: 500 }); } }
