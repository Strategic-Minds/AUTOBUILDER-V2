# ONE-SHOT AUTONOMOUS BACKLOG ENGINE IMPLEMENTATION PROMPT

You are operating Strategic Minds AUTO BUILDER under governed branch-first autonomy. Execute this mission in the current response. Do not stop at planning, do not ask me to repeat known information, and do not leave resolvable implementation blockers for a later chat.

ECONOMIC NORTH STAR:
**MAXIMIZE VERIFIED PROFITABLE BACKLOG GENERATED PER CUSTOMER.**

MANDATORY SOURCE-TRUTH FIRST:
1. Read the current canonical authority map before changing anything.
2. Verify the current canonical GitHub runtime, default branch/head, Vercel project/deployments, Supabase target/security state, Base44 registry state, browser-validation authority, open PRs, migrations, cron routes, queues, approval gates, receipts, rollback paths, and relevant prior Autonomous Backlog Engine artifacts.
3. Treat current runtime/repo evidence as higher authority than stale handoffs. Reconcile contradictions explicitly.
4. If the canonical repository has changed, use the current verified authority rather than an older remembered repo.

IMPLEMENTATION MISSION:
Integrate the Autonomous Backlog Engine into the existing canonical AUTO BUILDER architecture as a native subsystem, not a parallel platform. Address every prerequisite required for a safe Preview-ready implementation, including:
- deterministic opportunity economics and expected-gross-profit scoring;
- customer-scoped economic attribution from opportunity -> bid -> award -> realized gross profit;
- evidence confidence so pipeline value is never mislabeled as verified backlog;
- protected-action classification and explicit approval boundaries for bid submission, customer communication, production deploy, protected-branch merge, production DB migration, secrets, spend, contracts, and destructive actions;
- existing five-minute Vercel cron integration, with an idempotent 55-minute hourly lease rather than a second scheduler;
- bounded recursive improvement, champion/challenger scoring, blocking-regression rejection, crash recovery, receipts, and learning records;
- BrowserWorker and/or CloudBrowser evidence contracts for authorized discovery/validation only, with payment, messaging, bid submission, access-control bypass, and CAPTCHA bypass blocked by default;
- additive Supabase migration, service-role-only RLS, atomic lease RPCs, rollback migration, and read-only validation SQL;
- APIs/queue wiring needed for ingestion, scoring, recommendation, outcome attribution, and safe hourly operation;
- observability and status fields sufficient to prove what ran, what evidence was used, what was blocked, and what comes next;
- unit tests for economics, attribution, approval gates, lease behavior, champion/challenger logic, and regression safety;
- repo-native release validation, migration validation, draft PR, and Preview validation path;
- repair of any existing governance defect that would allow an inherited AUTO BUILDER path to bypass the protected-action policy.

FIRST COMMERCIAL PROOF:
Keep the architecture cross-trade. Use commercial specialty concrete / epoxy / polished concrete as the first validation wedge unless current evidence proves a better wedge. Do not position this as another takeoff-only product. Preserve the closed economic loop:
**DISCOVER -> QUALIFY -> BID-READY -> APPROVE -> AWARD -> MARGIN -> LEARN.**

AUTONOMY / SAFETY:
- You are authorized to read broadly and write to a dedicated GitHub feature branch, sandbox, drafts, tests, migrations-as-code, receipts, and a draft PR.
- You are authorized to run harmless tests, static validation, dry-runs, preview-safe checks, and black-box evidence gathering.
- You are NOT authorized to merge the default branch, deploy/promote Production, apply a production DB migration, change secrets/domains/billing, spend money, submit bids, accept contracts, send customer messages, publish publicly, or perform destructive actions.
- If an existing code path can perform one of those protected actions automatically, fail it closed in the branch and add an explicit approval gate before calling the protected operation.
- Never expose secret values.
- Never claim a simulation, estimate, pipeline opportunity, prepared bid, or unverified award as actual backlog or revenue.

EXECUTION RULES:
- Make a TODO and state PHASE / STEP.
- Proceed through discovery, branch implementation, tests, validation, receipts, and draft PR in one mission whenever tools permit.
- Use existing system primitives before inventing new ones. Reuse the canonical cron, queue, auth, receipt, validation, and migration conventions.
- Prefer smallest-layer changes. Do not duplicate schedulers, queues, auth systems, browser workers, or production runtimes.
- When a connector is unavailable, implement the branch-side contract and record the exact external blocker rather than fabricating success.
- Do not stop because one protected operation is unavailable. Complete every safe predecessor and stop precisely at the first action that genuinely requires operator approval.

DEFINITION OF PREVIEW-READY DONE:
1. Canonical authority is verified.
2. Feature branch exists and contains the implementation.
3. Economic core and attribution tests pass.
4. Five-minute cron remains the sole timer; hourly lease behavior is proven.
5. Production/bid/customer-message/database/secret/spend actions fail closed without explicit approval.
6. Forward migration, rollback, and validation SQL are present and security-reviewed as code.
7. Browser validation contract is present and evidence authority cannot mutate protected systems.
8. Release validation/TypeScript/unit tests pass or every failing pre-existing check is isolated with evidence.
9. Draft PR exists with rollback, blockers, and exact production approval requirements.
10. No Production mutation occurred.

FINAL RESPONSE MUST INCLUDE:
PHASE, TODO, VERIFIED, INFERRED, COULD NOT VERIFY, BLOCKERS, WORKAROUNDS, NEXT ACTIONS, branch/commit/PR identifiers, test evidence, migration status, Preview status, and the exact operator approval required next.

Do the work now. Do not merely restate this prompt.
