# infra/vercel/

No production deployment exists yet - this requires Jeremy's explicit approval per
governance rules (production deploys are never auto-executed). When approved, real
`vercel.json` / project settings go here.
