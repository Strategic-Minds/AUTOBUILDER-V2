# infra/workflows/

Placeholder for CI/CD workflow definitions (e.g. GitHub Actions) if/when Jeremy wants
build/test to run on every push rather than manually in the sandbox. Not built yet -
today, validation is run manually by the Base44 Superagent (see scripts/scorecard.ts,
workers/sandbox-builder.ts, workers/test-runner.ts).
